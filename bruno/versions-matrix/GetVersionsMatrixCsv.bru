meta {
  name: Get versions matrix - csv format
  type: http
  seq: 11
}

get {
  url: {{baseUrl}}/opencti/versions-matrix?format=csv 
  body: none
  auth: none
}

params:query {
  format: csv 
}

docs {
  Same matrix as "Get versions matrix", rendered as a two-row CSV: a header
  row (matching the JSON field names) followed by a single row of values.
  
  opencti_version,connector_mitreattack_version,connector_crowdstrike_version
  7.260807.0,6.240930.0,7.260309.0
  
  Content-Type: text/csv.
}
